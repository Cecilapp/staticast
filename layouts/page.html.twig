{% extends '_default/page.html.twig' %}

{% block head %}
{{ parent() }}
    <!--<link rel="stylesheet" href="{{ asset(['sass/garth.scss','sass/garth.scss'],{fingerprint:false,minify:false,filename:'css/style.css'})|url() }}">-->
    <meta property="og:image" content="{{ asset(site.podcast.image)|url({canonical: true}) }}" />
    <link rel="manifest" href="{{ url('manifest') }}">
{% include 'partials/regsw.js.twig' %}
    <!-- Netlify Identity -->
    <script src="https://identity.netlify.com/{{ site.netlify.identity.version|default('v1') }}/netlify-identity-widget.js"></script>
    {% apply minify_js %}<script>
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on('init', user => {
          if (!user) {
            window.netlifyIdentity.on('login', () => {
              document.location.href = "/{{ site.netlify.cms.path|default('admin') }}/";
            });
          }
        });
      }
    </script>{% endapply %}
{% endblock %}
