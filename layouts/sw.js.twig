{% extends 'serviceworker.js.twig' %}

{% block precache %}
// cache last 2 episodes
{% for file in site.static|filter(f => f.ext == 'mp3')|sort((a, b) => a.date|date('U') < b.date|date('U'))|slice(0,2) %}
urlsToCache.push('{{ url(file.path) }}');
{% endfor %}
{% endblock %}
