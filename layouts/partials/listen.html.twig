<style>
  #listen img {
    display: inline;
    height: 2.8rem;
  }
</style>
<div id="listen">
{% for service, url in site.podcast.listen %}
  <a href="{{ url }}"><img src="{{ asset('badge/'~ service ~'.svg')|url }}" alt="Listen on {{ service|capitalize }}"/></a>
{% endfor %}
</div>
