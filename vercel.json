{
  "build": {
    "env": {
      "PHP_VERSION": "7.4",
      "INSTALL_OPTIM": "true"
    }
  },
  "builds": [{
    "src": "vercel-build.sh",
    "use": "@vercel/static-build",
    "config": { "distDir": "_site" }
  }]
}
